requirejs.config({
    baseUrl: 'js/lib',
    paths: {
        app: '../app'
    }
});

// Start the main app logic.
requirejs(['jquery-1.10.2.min', 'underscrore-min', 'backbone-min'],
    function   ($, _, Backbone) {


    });